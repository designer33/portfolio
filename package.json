{
    "name": "portfolio-monorepo",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "npm run build --workspace=client"
    },
    "workspaces": [
        "client",
        "server"
    ],
    "devDependencies": {
        "vite": "^7.3.1"
    }
}