{
    "name": "portfolio-monorepo",
    "version": "1.0.0",
    "scripts": {
        "build": "npm install --prefix client && cd client && vite build"
    },
    "workspaces": [
        "client",
        "server"
    ],
    "dependencies": {
        "vite": "^7.3.1"
    }
}