{
    "name": "portfolio-monorepo",
    "version": "1.0.0",
    "scripts": {
        "build": "npm install && npm run build --workspace=client"
    },
    "workspaces": [
        "client",
        "server"
    ],
    "dependencies": {
        "vite": "^7.3.1"
    }
}